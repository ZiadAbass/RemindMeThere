# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Remind Me There' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for Remind Me There

	pod 'GoogleMaps'
	pod 'GooglePlaces'
	pod 'GooglePlacePicker'

  target 'Remind Me ThereTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Remind Me ThereUITests' do
    inherit! :search_paths
    # Pods for testing

  end

end
